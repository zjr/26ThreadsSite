//- Loads app/jade/layouts/default
include layouts/default
	block extend content
		-var height = 13600;
		-var panels = 11;
		div#main(style='height: #{height}px')
			header.header v0.1.0 Smooth Scroll Accomplished.
			#video-container
				img(
					src='/videos/img-seq/vid-0001.png',
					style='width: 100%; height: 100%;')
			-for(var i = 1; i <= panels; i++)
				- var pos = (i % 2 === 0) ? 'left' : 'right';
				- var rand = Math.random();
				- var speed = (i % 2 === 0) ? (rand+1).toFixed(1) : (rand+2).toFixed(1);
				- var position = ((1 / panels) * i).toFixed(1);
				.thread-spot(
					id='ts-#{i}',
					data-speed=speed
					data-position=position,
					style='#{pos}: 150px')
						##{i}
